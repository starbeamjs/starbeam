{
  "lint": {
    "command": "eslint --cache --max-warnings 0 ${package.root}",
    "cwd": "${workspace.root}"
  },
  "specs": {
    "command": "vitest --run",
    "cwd": "${package.root}"
  },
  "types": {
    "command": "tsc -b",
    "cwd": "${package.root}"
  }
}
