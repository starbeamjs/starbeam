{
  "lint": {
    "command": "turbo lint --output-logs errors-only",
    "cwd": "${package.root}"
  },
  "specs": {
    "command": "vitest --run",
    "cwd": "${package.root}"
  },
  "types": {
    "command": "tsc -b",
    "cwd": "${package.root}"
  }
}
