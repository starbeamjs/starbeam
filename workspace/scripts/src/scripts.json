{
  "lint": {
    "command": "turbo lint --output-logs errors-only",
    "cwd": "${package.root}"
  },
  "specs": {
    "command": "vitest --run",
    "cwd": "${package.root}"
  },
  "types": {
    "command": "turbo run typecheck --output-logs errors-only",
    "cwd": "${package.root}"
  }
}
